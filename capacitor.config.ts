import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.hostapp',
  appName: 'Host App with Native Features',
  webDir: 'www',
};

export default config;
